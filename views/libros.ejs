<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Libros — Juan Manuel Fructuoso Heredia</title>
  <link rel="stylesheet" href="/style.css" />
</head>
<body>

<nav class="nav">
  <div class="nav-inner">
    <a href="/" class="nav-brand">
      Juan Manuel Fructuoso Heredia
      <span>General (r) · Abogado · Escritor</span>
    </a>
    <ul class="nav-links">
      <li><a href="/">Inicio</a></li>
      <li><a href="/libros" class="active">Libros</a></li>
      <li><a href="/biografia">Biografía</a></li>
      <li><a href="/eventos">Eventos</a></li>
      <li><a href="/galeria">Galería</a></li>
      <li><a href="/registro" class="nav-cta">Registrarse</a></li>
    </ul>
  </div>
</nav>

<div class="container">
  <div class="page-header">
    <span class="section-label">Obra literaria</span>
    <h1>Libros</h1>
    <p>Narrativa basada en hechos reales — décadas de investigación convertidas en literatura de alto impacto.</p>
  </div>

  <div class="books-grid">
    <% libros.forEach(libro => { %>
    <div class="book-card">
      <div class="book-cover-placeholder">
        <span class="book-icon">📖</span>
        <span><%= libro.titulo %></span>
      </div>
      <div class="book-info">
        <p class="book-genre"><%= libro.genero %></p>
        <h3 class="book-title"><%= libro.titulo %></h3>
        <p class="book-excerpt"><%= libro.sinopsis %></p>
        <div class="book-meta">
          <span class="book-price"><%= libro.precio %></span>
          <div class="stars">
            <% for(let i = 1; i <= 5; i++) { %>
              <span class="star <%= i <= Math.round(libro.rating) ? 'filled' : 'empty' %>">★</span>
            <% } %>
            <span class="rating-count">(<%= libro.totalResenas %>)</span>
          </div>
        </div>
        <div style="display:flex; gap:10px;">
          <a href="/libros/<%= libro.id %>" class="btn btn-outline btn-sm">Ver detalles</a>
          <a href="<%= libro.enlaceAmazon %>" target="_blank" rel="noopener" class="btn btn-gold btn-sm">Amazon</a>
        </div>
      </div>
    </div>
    <% }) %>
  </div>

  <% if (resenas && resenas.length > 0) { %>
  <div style="margin-top:72px; border-top:1px solid var(--border); padding-top:60px;">
    <div class="section-header">
      <div>
        <span class="section-label">Lectores</span>
        <h2 class="section-title">Reseñas</h2>
      </div>
    </div>
    <div class="reviews-list">
      <% resenas.forEach(resena => { %>
      <div class="review-card">
        <div class="review-header">
          <div>
            <span class="review-author"><%= resena.usuario %></span>
            <div class="stars" style="margin-top:4px">
              <% for(let i = 1; i <= 5; i++) { %>
                <span class="star <%= i <= resena.rating ? 'filled' : 'empty' %>">★</span>
              <% } %>
            </div>
          </div>
          <span class="review-date"><%= resena.fecha %></span>
        </div>
        <p class="review-text"><%= resena.comentario %></p>
      </div>
      <% }) %>
    </div>
  </div>
  <% } %>
</div>

<footer class="footer">
  <div class="container">
    <div class="footer-name">Juan Manuel Fructuoso Heredia</div>
    <ul class="footer-links">
      <li><a href="/">Inicio</a></li>
      <li><a href="/libros">Libros</a></li>
      <li><a href="/biografia">Biografía</a></li>
      <li><a href="/eventos">Eventos</a></li>
      <li><a href="/galeria">Galería</a></li>
    </ul>
    <p class="footer-copy">&copy; <%= new Date().getFullYear() %> Juan Manuel Fructuoso Heredia. Todos los derechos reservados.</p>
  </div>
</footer>
</body>
</html>
